# Postman Artifacts

This directory will contain the Postman collection (`.json`) and environment (`.json`) required by REQUIREMENTS.md section 6, item 4, and section 14. Every request must receive the `X-Student-Id` header through a pre-request script.

TODO: add collection/environment artifacts after the suite is implemented. Confirm Postman account tier limits (Collection Monitors, Mock Servers) before committing to those features in the section 6 Postman-feature list.
