# FR-02 `POST /api/login` — Extended Test Cases (Audit Additions)

Confirmed additions from the `api-test-auditor` gap pass. Continues the `TC-` sequence
from `../master-test-cases.md` (TC-01..TC-35). None of these duplicate an existing row;
each closes a source-backed gap the generator's EP/BVA/State/Security/Schema templates
did not reach.

| TC ID | Technique | Precondition | Input / action | Expected result | Oracle | Trace | Miss category | Why the AI missed it |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-36 | State | Account `test@eshop.com`, counter=2 (two prior consecutive wrong-password attempts, e.g. mirroring the first two attempts of TC-16 on a fresh/reset test account) | `POST /api/login` with correct credentials (`Test1234!`) | `200 OK`, `token`+`user` returned (documented). Post-success counter state: UNSPECIFIED — record (do not assert) whether the counter resets to 0 | Response status/body of the success step; optionally a follow-up wrong-password probe to indirectly observe counter state (same technique as TC-19 step 2, applied from an S2 origin) | TR-06 (state-model.md S-06); srs.md FR-02 (silent on reset) | model limitation | TC-19 cites TR-06 in its Trace column without verifying its own precondition (counter=1, state S1) actually reaches the S2 precondition TR-06 requires — the cross-document connection between the state table and the master suite's precondition field was never checked |
| TC-37 | EP | Admin account `admin@eshop.com` exists, unlocked, counter=0 (srs.md section 1) | `POST /api/login` with `email="admin@eshop.com"`, `password="Admin123!"` | `200 OK`; body has `token` (string) and `user` (object); `user` must not contain a `password`/`passwordHash` field — same SEC-01 requirement applies regardless of role | Response status + body shape/fields, checking the same no-password invariant already verified for the non-admin account in TC-21 | srs.md section 1 (admin account); api_specification.md 1.2; srs.md SEC-01 line 278 | prompt quality | `domain-partition-catalog.md`'s EP class P-01 ("valid format, registered account") is instantiated only with `test@eshop.com`; principal/role type (user vs. admin) was never treated as its own equivalence dimension despite srs.md section 1 documenting two distinct default accounts with different roles |
| TC-38 | State (functional/integration) | Account `test@eshop.com`, unlocked, counter=0 | Step 1: `POST /api/login` with correct credentials, capture `token`. Step 2: `GET /api/users/me` with header `Authorization: Bearer <token>` | Step 1: `200 OK` + `token` (documented). Step 2: the issued token must be accepted by the protected endpoint per srs.md FR-02 line 43 ("Token được lưu phía client và gửi kèm tất cả các yêu cầu có xác thực qua header Authorization: Bearer <token>") — expect a non-401/403 response; exact success status/body of `GET /api/users/me` is UNSPECIFIED beyond api_specification.md 2.1's bare listing, but rejecting a freshly-issued token would directly contradict FR-02 line 43 | Response status of step 2; presence/absence of an auth-rejection status when using the step-1 token | srs.md FR-02 line 43; api_specification.md section 2.1 | model limitation | SEC-C-03 (security-cases.md) stops at structural JWT validation and never follows the cross-source connection to FR-02 line 43's separately documented requirement that the token must actually function against a protected endpoint |
| TC-39 | State (cross-account isolation) | `test@eshop.com` is LOCKED (fresh 3-failure sequence, e.g. immediately after TC-16); `admin@eshop.com` is unlocked, counter=0 | While `test@eshop.com` is locked, `POST /api/login` with `email="admin@eshop.com"`, `password="Admin123!"` | srs.md FR-02 line 42 says "tài khoản bị tạm khóa" (THE account is locked) — the lock is scoped to the specific account, not global/IP-wide; `admin@eshop.com`'s login must succeed normally (`200`, `token`+`user`), unaffected by `test@eshop.com`'s lock | Response status + presence of `token` for the admin request | srs.md FR-02 line 42 ("tài khoản bị tạm khóa" — per-account scope); state-model.md Notes (scope undocumented — this case tests the reading the SRS wording directly supports) | API characteristic | `state-model.md`'s Notes section flags the counter/lock scope (per-email vs. per-IP) as undocumented and stops there, without testing the one scope interpretation ("tài khoản" = the specific account) that the SRS wording itself directly supports |
| TC-40 | Security (concurrency / state-integrity) | Account test (disposable/reset), counter=0, unlocked | Fire 3 `POST /api/login` requests with wrong password concurrently (near-simultaneous, e.g. via parallel requests) against the same account | The documented invariant (srs.md FR-02 lines 41-42: counter +1 per failed attempt, lock triggers at >=3 consecutive failures) must hold under concurrent execution — the account must end up LOCKED after the 3 concurrent failures, with no lost-update leaving the counter at 1 or 2 | Final lock state (attempt a 4th correct-credential login immediately after — must be rejected if properly locked); response bodies of all 3 concurrent attempts | srs.md FR-02 lines 41-42 (invariant applies regardless of request timing) | API characteristic | The EP/BVA/State/Security/Schema templates used to produce TC-01..35 all model single sequential requests; none of the supporting catalogs considered that the counter-increment-then-check logic in FR-02 is a read-modify-write operation vulnerable to race conditions under concurrent load — a state-machine correctness property specific to this endpoint |
| TC-41 | Schema | — | `POST /api/login` with `email=null` (JSON `null` literal, not an omitted key, not an empty string), `password="Test1234!"` | Structured JSON error, not a `500` with a raw stack trace; exact status/shape UNSPECIFIED (same treatment as SC-12/SC-13) — but this is a distinct wire condition from TC-05 (empty string) and TC-06 (key omitted), since JSON deserializers/ORMs commonly handle `null` differently from `""` or a missing key | Response status + Content-Type + body parseability, same oracle pattern as SC-12/SC-13 | SC-12/SC-13 (schema-cases.md) establish the wrong-type facet template but only instantiate number/array; api_specification.md 1.2 (type implied) | prompt quality | `schema-cases.md`'s own scope note enumerates only two wrong-JSON-type facets (SC-12 number, SC-13 array); `null` is a third, commonly-mishandled JSON type distinct from both "wrong type" and "missing key" (TC-06) that the facet enumeration stopped short of including |
