# FR-02 `POST /api/login` — Extended Test Cases v2 (Pass 2)

Surviving Pass 1 additions (TC-36..TC-41), corrected per `audit-log-v2.md`, followed by
the gaps found in this pass (TC-42..TC-46). No Pass 1 addition was dropped, so the `TC-`
sequence has no gaps. Continues from `../master-test-cases.md` (TC-01..TC-35).

Contract tests below assert an outcome a source defines and count toward requirement
coverage. Characterization tests record an outcome no source defines; they expose the
specification gap and count toward input coverage only. Each row states which it is.

| TC ID | Technique | Precondition | Input / action | Expected result | Oracle | Trace | Miss category | Why the AI missed it |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-36 | State | Account `test@eshop.com`, counter=2 (two prior consecutive wrong-password attempts, e.g. mirroring the first two attempts of TC-16 on a fresh/reset test account), unlocked | `POST /api/login` with correct credentials (`Test1234!`) | `200 OK`, `token` + `user` returned (documented, api_specification.md 1.2 and srs.md line 43). Post-success counter state: UNSPECIFIED — record (do not assert) whether the counter resets to 0. That second observation is characterization and does not count as contract coverage of TR-06 | Response status/body of the success step; optionally a follow-up wrong-password probe to observe the counter state indirectly (same technique as TC-19 step 2, applied from an S2 origin) | `api_specification.md` 1.2; `srs.md` FR-02 line 43 (success path); `srs.md` FR-02 (silent on reset); TR-06 / `state-model.md` S-06 (cross-ref) | model limitation | TC-19 cites TR-06 in its Trace without checking that its own precondition (counter=1, state S1) reaches the S2 start state TR-06 requires — the cross-document connection between the state table and the master suite's precondition field was never verified |
| TC-37 | EP | Admin account `admin@eshop.com` exists, unlocked, counter=0 (srs.md section 1) | `POST /api/login` with `email="admin@eshop.com"`, `password="Admin123!"` | `200 OK`; body has `token` (string) and `user` (object); `user` must not contain a `password`/`passwordHash` field — the same SEC-01 requirement applies regardless of role. This row establishes `admin@eshop.com` as a usable second principal for TC-39 and TC-43; it asserts no invariant not already asserted by TC-01 and TC-21 and is not counted as unique coverage | Response status + body shape/fields, checking the same no-password invariant already verified for the non-admin account in TC-21 | `srs.md` section 1 (default accounts); `api_specification.md` 1.2; `specs/security-requirement.md` SEC-01 row | prompt quality | `domain-partition-catalog.md`'s EP class P-01 ("valid format, registered account") is instantiated only with `test@eshop.com`; the principal was never treated as a dimension at all, even though srs.md section 1 documents two distinct default accounts and later rows (TC-39, TC-43) need a second one to exist |
| TC-38 | State (functional/integration) | Account `test@eshop.com`, unlocked, counter=0 | Step 1: `POST /api/login` with correct credentials, capture `token`. Step 2: `GET /api/users/me` with header `Authorization: Bearer <token>` | Step 1: `200 OK` + `token` (documented). Step 2: the issued token must be accepted by the protected endpoint — srs.md line 43 requires the token to be sent with all authenticated requests, and api_specification.md section 2's preamble requires those APIs to be called with it, so rejecting a freshly issued token would contradict line 43. The exact success status and body of `GET /api/users/me` are UNSPECIFIED beyond section 2.1's bare listing and are not asserted | Response status of step 2 — specifically, whether it indicates auth rejection when using the step-1 token | `srs.md` FR-02 line 43; `api_specification.md` section 2 preamble and section 2.1 | model limitation | SEC-C-03 (`security-cases.md`) stops at structural JWT validation and never follows the cross-source connection to srs.md line 43, which separately requires the token to actually function against a protected endpoint |
| TC-39 | State (cross-account isolation) | `test@eshop.com` is LOCKED (fresh 3-failure sequence, e.g. immediately after TC-16); `admin@eshop.com` is unlocked, counter=0 | While `test@eshop.com` is locked, `POST /api/login` with `email="admin@eshop.com"`, `password="Admin123!"` | UNSPECIFIED — srs.md line 42's wording "tài khoản bị tạm khóa" supports a per-account reading, but the scope of the counter and lock (per email / per IP / global) is undocumented, and `specs/requirements.md` lists it under constraints not to invent. Record whether `admin@eshop.com` can log in while `test@eshop.com` is locked; do not treat either outcome as a defect by itself. Characterization | Response status + presence/absence of `token` for the admin request | `srs.md` FR-02 line 42; `specs/requirements.md` "Unspecified constraints (do not invent)" (counter scope); `state-model.md` Notes (cross-ref) | API characteristic | `state-model.md`'s Notes flags the counter and lock scope as undocumented and stops there, without building a case that would let execution discriminate between the readings |
| TC-40 | Security (concurrency / state integrity) | Account test (disposable/reset), counter=0, unlocked | Fire 3 `POST /api/login` requests with a wrong password concurrently (near-simultaneous, e.g. parallel requests) against the same account | The documented invariant must hold under concurrent execution: srs.md line 41 states the counter rises by exactly 1 per failed attempt and line 42 triggers the lock at >= 3 consecutive failures, which is a property of the attempt sequence and not of request timing. After 3 concurrent failures the account must be LOCKED, with no lost update leaving the counter at 1 or 2. No status code or body shape is asserted. Contract | Final lock state (a 4th correct-credential login immediately after must be rejected if the account is properly locked); response bodies of all 3 concurrent attempts | `srs.md` FR-02 lines 41-42 (invariant holds regardless of request timing) | API characteristic | The EP/BVA/State/Security/Schema templates behind TC-01..TC-35 all model single sequential requests; no catalog treats the counter-increment-then-check logic in FR-02 as a read-modify-write operation exposed to races |
| TC-41 | Schema | — | `POST /api/login` with `email=null` (JSON `null` literal — not an omitted key, not an empty string), `password="Test1234!"` | UNSPECIFIED — record the status, the `Content-Type`, whether the body parses as JSON, and whether it exposes a stack trace or an HTML error page; assert none of these. This is a distinct wire condition from TC-05 (empty string) and TC-06 (key omitted), since JSON deserializers commonly handle `null` differently from both. Characterization | Response status + `Content-Type` + body parseability, recorded not asserted | `api_specification.md` 1.2 (type implied by example); SC-12/SC-13 (`schema-cases.md`, cross-ref) | prompt quality | `schema-cases.md`'s own scope note enumerates exactly two wrong-JSON-type facets (SC-12 number, SC-13 array); `null` is a third, commonly mishandled type, distinct from both "wrong type" and "missing key", that the facet enumeration stopped short of |
| TC-42 | Schema / Security (message content) | Results of TC-17 (locked-account error body) and TC-03 (unknown-email error body) available from the same run. Analysis-only row — fires no new HTTP request | Compare the error body and message content of the locked-account rejection against the unknown-email rejection | The content must not let a client distinguish "this email exists and is currently locked" from "this email is not registered". srs.md line 42 attaches "không để lộ chi tiết nguyên nhân" to the lockout response in the same sentence that defines the lockout, so a lock message that reveals the account's existence leaks the cause. Contract | Content/message diff between the TC-17 and TC-03 responses | `srs.md` FR-02 line 42 | model limitation | TC-29 compares message *content*, but only across the two pre-lock causes (unknown email vs. wrong password); TC-28 includes the locked response, but only diffs top-level *key sets*. The generator split "content comparison" and "locked response" across two rows and never intersected them, even though the non-leak clause sits in the same sentence as the lockout rule it most directly governs |
| TC-43 | State (counter scope, interleaved) | Both `test@eshop.com` and `admin@eshop.com` fresh/reset, unlocked, counter=0 | Interleave 5 failed logins in this order: test (fail 1), admin (fail 1), test (fail 2), admin (fail 2), test (fail 3). Then probe each account separately with its correct credentials | UNSPECIFIED — record which reading the implementation follows. Under a per-account scope, `test@eshop.com` is LOCKED after its own 3rd failure while `admin@eshop.com` (2 failures) is not; under a per-IP or global scope, the lock triggers on the 3rd attempt overall and both are affected. Also record whether a failure on the other account breaks the "liên tiếp" (consecutive) chain for `test@eshop.com`. Do not assert either reading. Characterization | Lock state of each account, probed separately after the interleave (presence/absence of `token` with correct credentials) | `srs.md` FR-02 lines 41-42; `specs/requirements.md` "Unspecified constraints (do not invent)" (counter scope) | API characteristic | Every catalog models the counter as a single-account scalar; `state-model.md` Notes flags per-email vs. per-IP scope as undocumented and stops. No row exercises two accounts inside one failure sequence, which is the only arrangement that discriminates between the two readings |
| TC-44 | State (trigger guard) | Account `test@eshop.com`, fresh/reset, unlocked, counter=0 | Six sequential attempts: fail, fail, successful login (`Test1234!`), fail, fail, then a 6th attempt with correct credentials | The account must NOT be locked and the 6th attempt must return `200` + `token`. srs.md line 42 triggers the lock only on "đăng nhập sai từ 3 lần trở lên **liên tiếp**", and this sequence contains no run of 3 consecutive failures; srs.md line 43 then governs the successful attempt. The trigger condition is stated independently of how the counter is implemented, so this holds whether or not the counter resets on success — which is why an implementation that only tracks a raw counter will fail it. Contract | Response status + presence of `token` on the 6th attempt; the error class of attempts 4 and 5 recorded alongside | `srs.md` FR-02 line 42 ("liên tiếp"), line 43 | model limitation | `specs/requirements.md` files "whether the counter resets on successful login" under unspecified and stops there. The generator treated reset as the only open question and never derived that the word "liên tiếp" constrains the *trigger condition* on its own. TC-19 performs success-then-one-failure from counter=1, which never approaches the threshold and so cannot observe this guard |
| TC-45 | BVA / State (TTL renewal) | Account `test@eshop.com`, fresh/reset; lock triggered at T=0 by a fresh 3-failure sequence | At T=10s submit one wrong-password attempt; at T=15s submit another; then at T=31s submit correct credentials | The 31s attempt must succeed with `200` + `token`. srs.md line 42 documents the lock as 30 seconds from the lock event, so attempts made inside the window must not restart that clock; srs.md line 43 governs the successful attempt at T=31s. A rejection at T=31s means the effective lock exceeds the documented 30 seconds. The intervening attempts at T=10s and T=15s are expected to be rejected per line 42. Contract | Response status + presence/absence of `token` at T=31s; the T=10s and T=15s responses recorded as rejections | `srs.md` FR-02 lines 42-43 | API characteristic | BVA-01..BVA-03 (`state-model.md` S-10..S-12) all measure elapsed time from lock start with no intervening request inside the window, and TC-17 makes one attempt inside the window but never re-checks expiry afterwards. The generator modeled the TTL as a static duration and never considered a sliding-window implementation in which each rejected attempt refreshes it |
| TC-46 | EP x State intersection (security) | Account `test@eshop.com`, fresh/reset, unlocked, counter=0; TC-02's observed case-sensitivity result available | Three consecutive failed logins with a wrong password, each using a different case variant of the same email: `test@eshop.com`, `TEST@ESHOP.COM`, `Test@Eshop.com`. Then attempt with correct credentials using `test@eshop.com` | Conditional on TC-02. If TC-02 showed the email lookup is case-insensitive, all three attempts address the same account, so srs.md lines 41-42 require the account to be LOCKED and the final correct-credential probe to be rejected — a success there is a lockout bypass. If TC-02 showed the lookup is case-sensitive, only the exact-case variant reaches the account and the outcome is UNSPECIFIED; record it. Do not assert an outcome before TC-02's result is known. Contract in the case-insensitive branch, characterization otherwise | Lock state via the final correct-credential probe (presence/absence of `token`), read together with TC-02's recorded case behaviour | `srs.md` FR-02 lines 41-42 ("tài khoản"); depends on TC-02's recorded case-sensitivity observation | model limitation | `domain-partition-catalog.md` models email case (EC-02) and `state-model.md` models the lockout counter as independent dimensions, and nothing crosses them. The intersection is what matters here: whether the counter is keyed on the normalized or the raw submitted email decides whether the lockout can be bypassed by varying case |

## Coverage note

- Pass 1 additions retained: 6 (TC-36..TC-41), of which TC-38 and TC-40 are `VALID` and
  TC-36, TC-37, TC-39, TC-41 are `INCOMPLETE` with the corrections above applied.
- Pass 2 additions: 5 (TC-42..TC-46). Each is absent from the existing suite,
  source-backed, independently observable, and not a reworded duplicate.
- Contract tests among the Pass 2 additions: TC-42, TC-44, TC-45, and TC-46's
  case-insensitive branch. Characterization: TC-43, and TC-46's case-sensitive branch.
- TC-37 is retained for its supporting role only and is not counted as unique coverage.
