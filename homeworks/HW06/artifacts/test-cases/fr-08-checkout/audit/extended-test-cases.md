# FR-08 `POST /api/checkout` — Extended Test Cases (Audit Additions)

Six cases absent from the original `master-test-cases.md` (TC-01..TC-35), confirmed with
the user. Continues the `TC-` sequence as `TC-36..TC-41`.

| TC ID | Technique | Precondition | Input / action | Expected result | Oracle | Trace | Miss category | Why the AI missed it |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-36 | Security (EP) | User A exists (registered); a target cart exists for User A | `POST /api/checkout` with `Authorization: <valid-token-without-Bearer-prefix>` (raw token, no scheme), and as a second sub-case `Authorization: Basic dXNlcjpwYXNz` (wrong scheme entirely) | UNSPECIFIED exact status/body — same class as TC-12/TC-22 (malformed/invalid token); fail-closed rejection expected consistent with SEC-02, but no documented code | Response status + body for both sub-cases; no new order created; cart unchanged (same side-effect invariant as SEC-C-02) | `domain-partition-catalog.md` P-14 (EC-14, "wrong-scheme value" sub-variant); `api_specification.md` line 141; `srs.md` FR-08 line 104 | prompt quality | `domain-partition-catalog.md` P-14 itself names three EC-14 sub-variants ("garbage string, expired-shaped, or wrong-scheme value"), but the instantiation step (`master-test-cases.md` TC-12/TC-22, `security-cases.md` SEC-C-02) only ever materialized the "garbage string" member. The "wrong-scheme value" member named in the catalog's own row was never turned into a concrete payload anywhere in the 35-case suite. |
| TC-37 | Schema | TC-02..TC-10 and TC-13 (shipping_address/total_amount validation-error attempts) already fired; each target cart/order-count captured immediately before firing | Analysis-only row — for each of TC-02..TC-10 and TC-13, compare order count via `GET /api/orders/my-orders` and cart state via `GET /api/cart` immediately before and after the request fired | Regardless of the unresolved status/body shape, none of these validation-error attempts may have created an order or cleared/altered the cart — the same invariant `schema-cases.md` SC-08 already states for "Empty-cart / validation-error checkout attempt," but never instantiated for this field-level family (only for the empty-cart precondition case, via TC-31) | Before/after order-count and cart-state diff for each referenced TC | `schema-cases.md` SC-08; `srs.md` FR-08 line 108 (clearing documented only as a success side effect) | model limitation | `schema-cases.md`'s own SC-08 row states its scope as "Empty-cart / validation-error checkout attempt" — a title spanning both the empty-cart precondition case and the shipping_address/total_amount field-level validation errors — but the generator only cross-referenced SC-08 against the empty-cart case (TC-31) when assembling the master suite, never extending the same side-effect check to the parallel TC-02..TC-10/TC-13 family its own title names. |
| TC-38 | Schema / Lifecycle | TC-01 or TC-16 (successful checkout) already executed; User A's order count captured beforehand | Analysis-only row — `GET /api/orders/my-orders` as User A immediately after TC-01/TC-16's checkout call | A new order is present in the response reflecting the checked-out cart (order count increased by exactly one); UNSPECIFIED whether the item list, computed total, or status fields are echoed — record what is actually returned, do not assert an invented shape | Order count before/after; presence of a new order entry attributable to the just-completed checkout | `api_specification.md` 4.4 (`GET /api/orders/my-orders`); `srs.md` FR-08 lines 104-108 (order creation implied); FR-11 line 164 (order-history feature exists) | model limitation | None of the four generated catalogs (domain-partition, state-model, security-cases, schema-cases) ever treated `GET /api/orders/my-orders` as a general-purpose oracle surface for confirming a successful checkout actually persisted an order. TC-30 uses it narrowly to inspect one field (the total) for the tampered-amount scenario only, never as a direct existence/count check for the plain happy-path success case (TC-01/TC-16). |
| TC-39 | Security | User A and User B both logged in with separate carts; User A completes a successful checkout (e.g. immediately after TC-16) | `GET /api/orders/my-orders` as User B, immediately after User A's checkout | User B's order list must not contain the order just created by User A's checkout — order visibility is scoped per-user | User B's order list diffed before/after User A's checkout; the new order's id (if observable) confirmed absent from User B's response | `srs.md` FR-11 line 164 ("Người dùng chỉ xem được đơn hàng của chính mình" — user may only view their own orders); FR-07 lines 93-100 (cart is per-user) | model limitation | `security-cases.md` modeled cross-user isolation only on the write path (SEC-C-05, IDOR on checkout itself) and cited FR-07's per-user cart framing. It never connected FR-08's checkout-creates-an-order behavior to FR-11's explicit read-path scoping rule (srs.md line 164), so the equivalent read-side isolation check for order history was never generated. |
| TC-40 | Schema | TC-09 (negative/zero total_amount) response available; if either sub-case was accepted (created an order), that order/its total is inspectable | Analysis-only row — inspect the recorded/charged total for any order created by TC-09's `-1` or `0` sub-case | If TC-09 resulted in an order being created, its recorded/charged total must equal the real cart total, never the submitted `-1` or `0` — the same recompute rule already verified for the tampered-positive class in TC-30, extended to the negative/zero class | Field-level inspection of the total value wherever exposed (response body, `GET /api/orders/my-orders`, or `GET /api/orders/:id`), for any order TC-09 created | `srs.md` FR-08 line 107 (unconditional recompute rule); same trace basis as TC-30/SC-04 | model limitation | The generator connected FR-08 line 107's recompute rule to a companion verification row (TC-30) only for the EC-08 "tampered/lower positive value" class (TC-07/TC-26). Line 107's rule text is unconditional on submitted-value class, but the generator never extended the same TC-30-style companion check to the EC-10 negative/zero class exercised by TC-09. |
| TC-41 | Schema | User A logged in; cart non-empty | `POST /api/checkout` with a valid JSON body but `Content-Type: text/plain` (an incorrect, non-JSON value — distinct from TC-34's fully-omitted header) | UNSPECIFIED — neither source states server behavior for an incorrect (as opposed to missing) Content-Type value; record actual status/body, do not assume rejection or acceptance | Response status + Content-Type + body; inspected for raw stack trace/HTML per the same document-wide JSON convention as SC-10 | `schema-cases.md` SC-11 (only instantiates the "missing header" member of this dimension); `api_specification.md` (document-wide JSON convention) | prompt quality | `schema-cases.md` SC-11 and its sole instantiation TC-34 only cover the "header entirely omitted" member of the Content-Type dimension. The generator's schema-case derivation step never completed the standard three-way split for a header-driven EP dimension (missing / correct / incorrect-value), leaving the "incorrect value" member unaddressed. |
