# FR-08 `POST /api/checkout` — Master Test Case Suite

Continuous `TC-01..TC-35` sequence merging `domain-partition-catalog.md` (EP + BVA),
`state-model.md` (Lifecycle), `security-cases.md` (Security), and `schema-cases.md`
(Schema). Test accounts referenced: `test@eshop.com` / `Test1234!` (User A, per srs.md
section 1); a second registered account is referenced as User B for isolation cases
(`specs/requirements.md` and `state-model.md` do not name a fixed second account — any
distinct registered user with their own cart qualifies).

Where a documented status code, response body shape, or exact behavior does not exist in
either source, the Expected result / Oracle column says UNSPECIFIED and states what to
record instead of asserting an invented value.

| TC ID | Technique | Precondition | Input / action | Expected result | Oracle | Trace |
| --- | --- | --- | --- | --- | --- | --- |
| TC-01 | EP | User A logged in (valid token); cart has 2 distinct products (typical, non-empty) | `POST /api/checkout` with `shipping_address="123 Le Loi, TP.HCM"`, `total_amount` = the real cart total | Accepted — checkout proceeds; not asserted as a specific status code | Response status recorded; presence of any created-order signal | EC-01, EC-07, EC-12; api_specification.md 4.3 |
| TC-02 | EP | User A logged in; cart non-empty | `POST /api/checkout` with `shipping_address=""` (empty), valid `total_amount` | UNSPECIFIED — no source states whether an empty address is rejected | Response status + body | EC-02; `specs/requirements.md` |
| TC-03 | EP | User A logged in; cart non-empty | `POST /api/checkout` with the `shipping_address` key omitted, valid `total_amount` | UNSPECIFIED — required/optional status not documented | Response status + body | EC-03; `specs/requirements.md` |
| TC-04 | EP | User A logged in; cart non-empty | `POST /api/checkout` with `shipping_address=null`, valid `total_amount` | UNSPECIFIED | Response status + body | EC-04; `specs/requirements.md` |
| TC-05 | EP | User A logged in; cart non-empty | `POST /api/checkout` with `shipping_address` = a 5,000-character string, valid `total_amount` | UNSPECIFIED — no documented maximum length exists to violate | Response status + body | EC-05; `equivalence-partitioning-and-bva.md` (do not invent a max length) |
| TC-06 | EP | User A logged in; cart non-empty | `POST /api/checkout` with `shipping_address=12345` (JSON number, not string), valid `total_amount` | UNSPECIFIED — wrong-type class | Response status + body | EC-06; api_specification.md 4.3 (type implied by example) |
| TC-07 | EP | User A logged in; cart non-empty with a real, known total (e.g. 500,000₫) | `POST /api/checkout` with valid `shipping_address`, `total_amount=1` (does not match real cart total) | Documented: server recomputes the total itself; the submitted value is not authoritative for the resulting total | Response status/body; resulting order's total (if inspectable) inspected further in TC-30 | EC-08; srs.md FR-08 line 107; composite with SEC-C-06 |
| TC-08 | EP | User A logged in; cart non-empty | `POST /api/checkout` with valid `shipping_address`, `total_amount` key omitted | UNSPECIFIED whether the request is rejected for the missing field before recompute, or proceeds since the value is documented as ignored | Response status + body | EC-09; `specs/requirements.md` |
| TC-09 | EP | User A logged in; cart non-empty | `POST /api/checkout` with valid `shipping_address`, `total_amount=-1`, then repeat with `total_amount=0` | UNSPECIFIED whether rejected by a validation layer or silently ignored per the recompute rule | Response status + body, both sub-cases | EC-10; srs.md FR-08 line 107; `specs/requirements.md` |
| TC-10 | EP | User A logged in; cart non-empty | `POST /api/checkout` with valid `shipping_address`, `total_amount="abc"` (string, not number) | UNSPECIFIED — wrong-type class | Response status + body | EC-11; `specs/requirements.md` |
| TC-11 | EP | User A has a valid session (token exists but is not sent) | `POST /api/checkout` with the `Authorization` header entirely absent | Rejected — auth required; exact status UNSPECIFIED | Response status + body | EC-13; api_specification.md line 141; srs.md FR-08 line 104; composite with SEC-C-01 |
| TC-12 | EP | User A exists | `POST /api/checkout` with `Authorization: Bearer not-a-real-token` | Rejected — auth required; exact status UNSPECIFIED | Response status + body | EC-14; api_specification.md line 141; composite with SEC-C-02 |
| TC-13 | EP (combination) | User A logged in; cart non-empty | `POST /api/checkout` with `shipping_address=""` (empty) **and** `total_amount` key omitted simultaneously | UNSPECIFIED — still an unresolved required-field/validation question for a combination of causes; must not reveal materially more diagnostic detail than a single-field violation | Response status + body, compared for shape against TC-02 and TC-08 | EC-02, EC-09 (combination); `specs/requirements.md` |
| TC-14 | BVA | User A logged in; cart has exactly 0 items (LB) | `POST /api/checkout` with valid `shipping_address`, valid-looking `total_amount` (e.g. `0`) | UNSPECIFIED — no source states whether checkout on an empty cart is rejected or otherwise handled | Response status + body; side-effect checked further in TC-31 | BVA-01 (state-model.md P-15); srs.md FR-07 line 100 |
| TC-15 | BVA | User A logged in; cart has exactly 1 item, quantity 1 (LB+1, smallest non-empty cart) | `POST /api/checkout` with valid `shipping_address`, `total_amount` matching the single item's price | Documented: checkout may proceed; order created from the single cart line; cart cleared on success | BVA-02 (domain-partition-catalog.md P-16); srs.md FR-08 lines 104-108 |
| TC-16 | Lifecycle | User A logged in; cart non-empty (fresh, not yet checked out) | `POST /api/checkout` with valid `shipping_address` and matching `total_amount`; then `GET /api/cart` to verify clearing | Order created from cart contents; follow-up `GET /api/cart` shows an empty cart | Response of the checkout call + response of the follow-up `GET /api/cart` | S-01; srs.md FR-08 lines 104-108 |
| TC-17 | Lifecycle | Immediately after TC-16's success (cart is now empty as a direct result) | `POST /api/checkout` again with the same valid `shipping_address`/`total_amount` shape | UNSPECIFIED — reject with an error, silent no-op, or an empty order; srs.md is silent on repeat-call behavior | Response status + body of the repeat call | S-02; srs.md FR-08 (silent) |
| TC-18 | Lifecycle | A brand-new user account (never added anything to cart, never checked out) | `POST /api/checkout` with valid `shipping_address`/`total_amount` on the never-populated cart | UNSPECIFIED — same undocumented outcome class as TC-17; compared against TC-17 to check consistency across the two different empty-cart reachability paths | Response status + body, compared against TC-17 | S-03; srs.md FR-08 (silent); FR-07 line 100 |
| TC-19 | Lifecycle | User A logged in; cart non-empty | Issue two `POST /api/checkout` calls back-to-back before either response is observed (double-submit) | UNSPECIFIED — no documented idempotency/locking rule; at most one call plausibly succeeds if clearing is atomic, but this is an inference, not an asserted rule | Both responses; final cart state; count of orders created | S-04; srs.md FR-08 lines 104-108 (single documented transition only) |
| TC-20 | Lifecycle | User A and User B are both logged in, each with their own non-empty, distinct cart | User A calls `POST /api/checkout` (normal, no adversarial payload); then inspect User B's cart and orders | User A's checkout must not alter User B's cart contents or existing orders | User B's cart/order state compared before and after User A's call | S-05; srs.md FR-07 lines 93-100; FR-08 lines 104-108 |
| TC-21 | Security | Anonymous (no credentials); a target cart exists for some user | `POST /api/checkout` with the `Authorization` header entirely absent | Fail-closed: request rejected; no order created; target cart is not cleared; no internal error detail leaked | No new order exists; cart unchanged; response body inspected for leaked detail | SEC-C-01; api_specification.md line 141; srs.md FR-08 line 104 |
| TC-22 | Security | Anonymous, presenting a garbage credential; a target cart exists | `POST /api/checkout` with `Authorization: Bearer not-a-real-token` | Fail-closed: rejected; no order created; cart unchanged | No new order exists; cart unchanged | SEC-C-02; api_specification.md line 141 |
| TC-23 | Security | Attacker holds a forged/tampered JWT (valid-looking header/payload, invalid signature); a target cart exists | `POST /api/checkout` with that forged token | Fail-closed: rejected; no order created; cart unchanged | No new order exists; cart unchanged | SEC-C-03; api_specification.md line 141 |
| TC-24 | Security | User A's token has expired (constructed as expired at execution time); User A's cart is non-empty | `POST /api/checkout` with the expired token | Fail-closed: rejected; no order created; cart unchanged | No new order exists; cart unchanged | SEC-C-04; api_specification.md line 141; token expiry itself flagged unspecified in `specs/requirements.md` |
| TC-25 | Security | User A and User B both logged in with separate non-empty carts (different item sets) | `POST /api/checkout` using User A's valid token, with an extraneous `"user_id": <B's id>"` (or `"cart_id": <B's cart id>"`) field added to the body | Server derives the owner from the JWT only, ignoring the injected field; resulting order (if any) reflects only User A's cart; User B's cart/orders are unchanged | User B's cart/orders diffed before/after; resulting order's contents compared against User A's cart, not User B's | SEC-C-05; srs.md FR-07 lines 93-100; FR-08 lines 104-108 (no cart/user id parameter documented) |
| TC-26 | Security | User A logged in; cart non-empty with a real, known total (e.g. 500,000₫) | `POST /api/checkout` with valid `shipping_address`, `total_amount=1` | Server ignores the submitted total; resulting order's recorded/charged total (via response, `GET /api/orders/my-orders`, or `GET /api/orders/:id`) equals the real cart total, never `1` | Recorded/charged total inspected via whichever surface exposes it | SEC-C-06; srs.md FR-08 line 107; composite with EC-08 (TC-07) and SC-04 (TC-30) |
| TC-27 | Security | User A logged in; cart non-empty | `POST /api/checkout` with `shipping_address="' OR '1'='1"` (boolean-tautology probe), valid `total_amount` | Parameterized query treats the payload as an inert literal string; no raw SQL error surfaced; no unauthorized data returned/altered | Response status/body inspected for SQL error text or data leakage | SEC-C-07; srs.md line 282 (SEC-05) |
| TC-28 | Security | User A logged in; cart non-empty | `POST /api/checkout` with `shipping_address="123 Le Loi'; --"` (comment-truncation probe), valid `total_amount` | Same control and oracle as TC-27 | Response status/body inspected for SQL error text or data leakage | SEC-C-08; srs.md line 282 (SEC-05) |
| TC-29 | Schema | TC-01/TC-16 (successful checkout) response available | Inspect the raw success response: status code, `Content-Type`, body presence/parseability, and any status-like field | Record actual status code and media type (no documented value to assert against); if a body exists, confirm it is parseable JSON; if a status/state-like field is present, note whether it reads `pending`, as weakly inferable from FR-10's diagram topology | Structural inspection of TC-01/TC-16's raw response | SC-01, SC-02, SC-03, SC-05; api_specification.md 4.3 (no example given); srs.md FR-10 lines 141-156 |
| TC-30 | Schema | TC-07/TC-26 (tampered `total_amount`) response and any order-detail surface available | Inspect the recorded/charged total for the order created in TC-07/TC-26 | Recorded/charged total equals the real cart total, never the submitted `1` | Field-level inspection of the total value wherever it is exposed | SC-04; srs.md FR-08 line 107; composite with security coverage (SEC-C-06) |
| TC-31 | Schema | TC-14 (empty-cart checkout attempt) response available | Inspect the empty-cart rejection/response: status code and body shape; then confirm no order was created and the (still-empty) cart is unaffected | UNSPECIFIED status/envelope shape; side-effect invariant is assertable regardless: no order created, no cart-clear side effect beyond the pre-existing empty state | Structural inspection + before/after order-count and cart-state check | SC-06, SC-07, SC-08; srs.md FR-08 line 108 (side-effect only documented for success) |
| TC-32 | Schema | TC-11/TC-21 (missing-auth) response available | Inspect the auth-rejected response and confirm no order was created for the targeted cart | Side-effect invariant: no order created, no cart cleared, regardless of the unresolved status/body shape | Before/after order-count and cart-state check | SC-09; api_specification.md line 141; srs.md FR-08 line 108 |
| TC-33 | Schema | User A logged in; cart non-empty | `POST /api/checkout` with a syntactically broken JSON body (e.g. trailing comma / unclosed brace) | UNSPECIFIED exact status/shape; must not be a raw stack trace or unparseable/HTML error page | Response status + `Content-Type` + body parseability | SC-10; api_specification.md (document-wide JSON convention) |
| TC-34 | Schema | User A logged in; cart non-empty | `POST /api/checkout` with a valid JSON body but no `Content-Type: application/json` request header | UNSPECIFIED — neither source states server behavior for this case | Response status + `Content-Type` + body | SC-11; api_specification.md 4.3 (silent) |
| TC-35 | Schema | TC-02 (empty `shipping_address`) and TC-14 (empty cart) responses available | Compare the two error response bodies' top-level key sets | UNSPECIFIED whether both share the same envelope shape; recorded as an internal-consistency observation, not an assertion of a specific documented shape | Structural diff of TC-02 vs. TC-14 response bodies | SC-12; api_specification.md (document-wide gap); mirrors `fr-02-login` TC-28 pattern |

## Coverage Gate

- Total: 35/35
- By technique: EP=13, BVA=2, State/Lifecycle=5, Security=8, Schema=7
- Parameters without EP/BVA coverage: none — `shipping_address` (EC-01..06), `total_amount`
  (EC-07..11), and the `Authorization` header (EC-12..14) each have every catalog class
  represented in TC-01..TC-13; cart-count boundary (BVA-01, BVA-02) covered in TC-14..TC-15
- Applicable SEC ids without cases: none — SEC-02 (TC-11/TC-21, TC-12/TC-22, TC-23, TC-24
  composite), SEC-02-extended(a) IDOR (TC-25), SEC-02-extended(b) recompute (TC-07/TC-26,
  TC-30 composite), SEC-05 (TC-27, TC-28)
- Valid transitions: not applicable — this endpoint has no explicit/implicit state table
  (`state-model.md` classification: lifecycle adaptation). Lifecycle coverage instead:
  5/5 scenario ids covered (S-01 TC-16; S-02 TC-17; S-03 TC-18; S-04 TC-19; S-05 TC-20);
  missing: none
- Documented response variants: 0/0 — api_specification.md 4.3 documents no response
  example for this endpoint (request example only); this is reported as a specification
  gap, not a coverage shortfall. All 12 `SC-*` ids trace to at least one TC row
  (TC-29..TC-35) despite the documentation gap
- Shortfall: none. All 14 EC ids, both BVA ids, all 5 lifecycle scenario ids, all 8
  applicable SEC-C ids, and all 12 SC ids trace to at least one TC row. No reworded
  duplicates were added to reach the 35 target — TC-07/TC-26/TC-30, TC-11/TC-21,
  TC-12/TC-22, TC-14/TC-31, and TC-02/TC-35 are the only same-request cross-references,
  and each paired row asserts a distinct oracle (accept/reject class vs. security
  fail-closed/side-effect check vs. schema/envelope inspection) not covered by the other.
